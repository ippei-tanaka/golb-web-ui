import React, { PropTypes } from 'react';
import Menu from './Menu';

const Dashboard = (props) => {

    return (
        <div>
            <Menu/>
            Dashboard
        </div>
    );
};

export default Dashboard;