import React from 'react';
import Layout from './Layout';

const Home = () => {
    return (
        <Layout>
            <p className="module-temp">Home</p>
        </Layout>
    );
};

export default Home;