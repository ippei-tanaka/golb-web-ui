import React from 'react';
import Header from '../containers/Header';

const Home = (props) => {
    return (
        <div>
            <Header/>
            <p className="module-temp">Home</p>
        </div>
    );
};

export default Home;