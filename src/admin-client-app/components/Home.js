import React, { PropTypes } from 'react';
import Header from './Header';

const Home = (props) => {
    return (
        <div>
            <Header/>
            Home
        </div>
    );
};

export default Home;