import React from 'react';
import Header from '../containers/Header';

const Home = (props) => {
    return (
        <div>
            <Header/>
            Home
        </div>
    );
};

export default Home;