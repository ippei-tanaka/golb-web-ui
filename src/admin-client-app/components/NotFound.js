import React, { PropTypes } from 'react';
import Menu from './Menu';

const NotFound = (props) =>
{
    return (
        <div>
            <Menu/>
            Not Found
        </div>
    );
};

export default NotFound;